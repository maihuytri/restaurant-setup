import ReservationList from "../components/ReservationList";

const Reservations = () => {
  return (
    <div className="p-3">
      <ReservationList />
    </div>
  );
};

export default Reservations;
